{{- if .Values.migrateJob.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "paidiver-annotations-api.fullname" . }}-migrate
  labels:
    {{- include "paidiver-annotations-api.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: "{{- if .Values.migrateJob.runOnUpgrade -}}{{- if .Values.migrateJob.runOnInstall -}}post-install,post-upgrade{{- else -}}post-upgrade{{- end -}}{{- else -}}{{- if .Values.migrateJob.runOnInstall -}}post-install{{- else -}}post-install{{- end -}}{{- end -}}"
    helm.sh/hook-weight: "-1"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  backoffLimit: {{ .Values.migrateJob.backoffLimit }}
  template:
    spec:
      restartPolicy: Never
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      initContainers:
        - name: wait-for-postgres
          image: "{{ .Values.api.image.repository }}:{{ .Values.api.image.tag }}"
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          command:
            - sh
            - -ec
            - |
              echo "Waiting for PostgreSQL..."
              until pg_isready \
                -h $POSTGRES_HOST \
                -p $POSTGRES_PORT \
                -U $POSTGRES_USER; do
                sleep 2
              done
              # create the PostGIS extension if it doesn't exist
              echo "Creating PostGIS extension if it doesn't exist..."
              psql "host=$POSTGRES_HOST port=$POSTGRES_PORT dbname=$POSTGRES_DB user=$POSTGRES_USER password=$POSTGRES_PASSWORD" -c "CREATE EXTENSION IF NOT EXISTS postgis;"
              echo "Waiting for PostGIS extension to exist..."
              until psql "host=$POSTGRES_HOST port=$POSTGRES_PORT dbname=$POSTGRES_DB user=$POSTGRES_USER password=$POSTGRES_PASSWORD" -tAc "SELECT 1 FROM pg_extension WHERE extname = 'postgis'" | grep -q 1; do
                sleep 2
              done
          env:
            {{- include "paidiver-annotations-api.dbEnv" . | nindent 12 }}
      containers:
        - name: migrate
          image: "{{ .Values.migrateJob.image.repository }}:{{ .Values.migrateJob.image.tag }}"
          imagePullPolicy: {{ .Values.migrateJob.image.pullPolicy }}
          env:
            {{- include "paidiver-annotations-api.dbEnv" . | nindent 12 }}
            - name: DJANGO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.api.env.djangoSecretKey.secretName }}
                  key: {{ .Values.api.env.djangoSecretKey.secretKey }}
            - name: DJANGO_DEBUG
              value: "{{ .Values.api.env.djangoDebug }}"
          command:
            - sh
            - -ec
            - |
              python manage.py migrate
          securityContext:
            {{- toYaml .Values.api.securityContext | nindent 12 }}
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
      imagePullSecrets:
        - name: {{ .Values.api.image.imagePullSecret }}
{{- end }}
